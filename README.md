# Generates Config Workbook using Force.com CLI tool

## Login to the desired org using Force.com CLI before running the script

## Copy the script to the directory where you want to generate the files